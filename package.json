{
	"name": "nodegui-template-esbuild-ts",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "node build --prod",
		"start": "node build && cd dist && qode ./app.js",
		"debug": "node build && cd dist && qode --inspect ./app.js"
	},
	"type": "module",
	"dependencies": {
		"@nodegui/nodegui": "^0.41.0"
	},
	"devDependencies": {
		"@nodegui/packer": "^1.5.0",
		"@types/node": "^16.4.13",
		"esbuild": "^0.13.15"
	}
}
